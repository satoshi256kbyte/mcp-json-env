{
  "servers": {
    "template-remote": {
      "type": "http",
      "url": "${env:MCP_SERVER_URL}",
      "headers": {
        "Authorization": "Bearer ${env:MCP_SERVER_API_KEY}"
      }
    }
  }
}
