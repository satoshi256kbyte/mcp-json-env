[project]
name = "mcp-json-env-devcontainer"
version = "0.1.0"
description = "Devcontainer template for MCP JSON files with environment variables"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.uv]
package = false
